# Swift UI project

1. Crear un nuevo proyecto. A la hora de crear la app, seleccionar como interfaz SwiftUI en vez de Storyboard.

En swift UI ya no hay Controllers ni Storyboard. La app se inicia en la classe que tenga @main definido.